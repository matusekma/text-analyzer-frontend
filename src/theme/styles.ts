import { extendTheme } from "@chakra-ui/react"

const theme = extendTheme({
    styles: {
        global: {
            // styles for the `body`
            body: {
                
            },
            // styles for the `a`
            a: {
                
            },
        },
    },
})

export default theme;